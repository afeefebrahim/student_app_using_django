{% extends "student_app/base.html" %}

{% block title %}Add a New student details{% endblock %}

{% block body_block %}
    <div class="hero-unit">
        <h1>Add a student details</h1>
        <div class="container">
        <form class="span6" id="student_form" method="post" action="/student_app/add_details/">
            {% csrf_token %}
            <table>
            {% for field in form.visible_fields %}
                <tr>
                {{ field.errors }}
                <td>{{ field.help_text }}</td>
                <td>{{ field }}</td>
            </trs>
            {% endfor %}
        </table>
            <input class="btn btn-primary" type="submit" name="submit" value="save" />
        </form>
    </div>
</div>
{% endblock %}